{
  "name": "itoa-many",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "asbuild:untouched": "asc assembly/index.ts -b build/untouched.wasm -t build/untouched.wat --importMemory --sourceMap --validate",
    "asbuild:optimized": "asc assembly/index.ts -b build/optimized.wasm -t build/optimized.wat --importMemory --sourceMap --validate --optimize --noDebug",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized"
  },
  "author": "Francis Crick <fcrick@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "assemblyscript": "github:fcrick/assemblyscript#v1"
  }
}
